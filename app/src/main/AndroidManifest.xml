<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.SistemaFerramentas"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CadastrarActivity" android:exported="false" />
        <activity android:name=".AlterarActivity" android:exported="false" />
        <activity android:name=".ConsultarActivity" android:exported="false" />
        <activity android:name=".ExcluirActivity" android:exported="false" />

    </application>

</manifest>
